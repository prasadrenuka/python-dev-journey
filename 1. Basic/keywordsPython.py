import keyword
print(len(keyword.kwlist))  # Shows all Python keywords
print(keyword.kwlist)

# Notes
'''
function: called on it's own
method: belongs to an object, called on something (ex: 'hello'.upper())
simple rule: Has a dot before it --> method, standlone > function

print() is a function
len () is a function

packages: external library you bring in when needed (folder of .py files is package)
module: single .py file module

keyword is module not package because it's built-in in python
'''
